HYBRID CHALLENGE LOG — GitHub Pages version

Replace/upload these files in the ROOT of the GitHub repository:
  index.html
  manifest.webmanifest
  service-worker.js
  icon-192.png
  icon-512.png
  apple-touch-icon.png

Keep your existing config.js unchanged.

Then on GitHub:
1. Repository → Settings → Pages
2. Build and deployment → Source: Deploy from a branch
3. Branch: main
4. Folder: /(root)
5. Save

Expected public URL:
https://wenting0429.github.io/hybrid-challenge-log/

After GitHub Pages finishes deploying:
- Android Chrome / Samsung Internet:
  open the GitHub Pages URL, then choose Install app / Add to Home screen.
- iPhone Safari:
  Share → Add to Home Screen.

Netlify is not needed for this version.
Supabase remains the cloud data backend.

Important:
Use the GitHub Pages URL when installing the app.
If an old installed shortcut remains, remove it first and install again from the GitHub Pages URL.
